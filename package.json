{
    "name": "monitoring-service-task",
    "private": true,
    "packageManager": "npm@10.2.4",
    "workspaces": [
        "apps/*"
    ],
    "scripts": {
        "build": "turbo run build",
        "dev": "turbo run dev",
        "lint": "turbo run lint",
        "test": "turbo run test"
    },
    "devDependencies": {
        "turbo": "^2.0.0"
    },
    "engines": {
        "node": ">=18"
    },
    "dependencies": {
        "systeminformation": "^5.31.3"
    }
}
